#! bin/bash

mkdir -p $HOME/.local/bin/
export PATH="$PATH:$HOME/.local/bin/"

# download vscode plugin and put in ~/.theia/plugins
# download svls and put in ~/.local/bin
# install verilator
# configure settings.json in ~/.theia